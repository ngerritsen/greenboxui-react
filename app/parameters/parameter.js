import Immutable from 'immutable';

export default Immutable.Record({
    controlInstanceId: null,
    parameterId: null,
    name: null,
    value: null,
    unit: null,
    dirty: null,
    isSet: false
});